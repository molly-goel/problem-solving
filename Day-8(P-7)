#include <bits/stdc++.h>

using namespace std;

int main()
{
   int n, i,j,count=0;
   cin>>n;
   for(i=2;i<=n;i++)
     {
        if(n%i==0)
        {count=0;
           for(j=1;j<=i;j++)
           {
               if(i%j==0)
               count=count+1; 
            }
           if(count==2)
           {
               cout<<i<<endl;
           }
       
         }
     }
    return 0;
}
